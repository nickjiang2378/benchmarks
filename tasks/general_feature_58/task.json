{
 "id": "general_feature_58",
 "category": "general_feature",
 "type": "fix",
 "repo": "pydantic/pydantic",
 "base_commit": "7daa2cd3fd24ac51cc98cb202d28faa9a4c94dd9",
 "instructions": "The repository at /repo is a Python library. Below is a bug report filed\nagainst it. Modify the library source to resolve the reported problem. Do NOT\nmodify anything under tests/ -- changes there are ignored by scoring. Your\nchange is graded by tests that are not present in the repository: partial\ncredit for the fraction of currently-failing tests your change makes pass,\nwith a penalty if you break tests that currently pass.",
 "prompt": "# Pydantic FilePath does not accept sockets\n\n### Description\n\nI want to pass a file path to an existing unix domain socket to a pydantic class and have it validate that the path exists.\nfrom linux's perspective, its the same as any other file.\neither it exists or it doesn't.\n\nPydantic seems to draw the distinction, actually looking at the type of the file object, and sees that its a socket and not a concrete file.\n\nStrangely, Pydantic accepts symlinks to real files even though symlinks themselves are not real files either :stuck_out_tongue: \n```\n>       Reproduction(host=\"/tmp/tmp_socket\")  # explodes\nE       pydantic_core._pydantic_core.ValidationError: 1 validation error for Reproduction\nE       host\nE         Path does not point to a file [type=path_not_file, input_value='/tmp/tmp_socket', input_type=str]\n```\n\n### Example Code\n\n```Python\ndef test_pydantic_socket_validation():\n    from pathlib import Path\n    from pydantic import BaseModel, FilePath\n    import socket\n\n\n    class Reproduction(BaseModel):\n        host: FilePath\n\n    # clean up after ourselves\n    socket_path = Path(\"/tmp\")/\"tmp_socket\"\n    socket_path.unlink(missing_ok=True)\n    # create a socket\n    sock = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n    sock.bind(\"/tmp/tmp_socket\")\n\n    # create the real files for comparison\n    real_file = Path(\"/tmp\") / \"foo.txt\"\n    real_file.unlink(missing_ok=True)  # clean up after ourselves\n    real_file.touch()\n    link_to_real_file = Path(\"/tmp\") / \"foo.lnk.txt\"\n    link_to_real_file.unlink(missing_ok=True)  # clean up after ourselves\n    link_to_real_file.symlink_to(real_file)\n    # assert real path to real file works\n    Reproduction(host=\"/tmp/foo.txt\")\n    # assert symlinked path to real file works\n    Reproduction(host=\"/tmp/foo.lnk.txt\")\n    # assert real path to socket works\n    Reproduction(host=\"/tmp/tmp_socket\")  # explodes\n```\n\n### Python, Pydantic & OS Version\n\n```Text\npydantic version: 2.9.1\n        pydantic-core version: 2.23.3\n          pydantic-core build: profile=release pgo=false\n                 install path: /PATH_REDACTED/.venv/lib/python3.12/site-packages/pydantic\n               python version: 3.12.5 (main, Aug 14 2024, 05:08:31) [Clang 18.1.8 ]\n                     platform: Linux-6.5.0-44-generic-x86_64-with-glibc2.35\n             related packages: typing_extensions-4.12.2\n                       commit: unknown\n```"
}