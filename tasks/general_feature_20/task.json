{
 "id": "general_feature_20",
 "category": "general_feature",
 "type": "fix",
 "repo": "pydantic/pydantic",
 "base_commit": "f334963c7e304d1dc4ef4fa5955e87cd6a11c750",
 "instructions": "The repository at /repo is a Python library. Below is a bug report filed\nagainst it. Modify the library source to resolve the reported problem. Do NOT\nmodify anything under tests/ -- changes there are ignored by scoring. Your\nchange is graded by tests that are not present in the repository: partial\ncredit for the fraction of currently-failing tests your change makes pass,\nwith a penalty if you break tests that currently pass.",
 "prompt": "# Fields with `exclude_if` should not be required in serialization JSON schema\n\n### Description\n\nI've been happily using the new `exclude_if` feature, but it's not quite behaving as expected.\n\nExpectation: the JSON schema in serialization mode marks fields with `exclude_if` as optional. The JSON schema accurately describes the structure of the serialization output.\n\nReality: such fields are marked as required, even though they may be absent in the actual output.\n\nFor example, consider this model:\n\n```python\nclass MyModel(pydantic.BaseModel):\n    field: Annotated[str, pydantic.Field(exclude_if=lambda x: x == \"null\")]\n```\n\nIts input and output JSON schema are as follows, with the `field` marked as `required`:\n\n```json5\n{\"properties\": {\"field\": {\"title\": \"Field\", \"type\": \"string\"}},\n \"required\": [\"field\"],  // <--\n \"title\": \"MyModel\",\n \"type\": \"object\"}\n```\n\nHowever, dumping values like `MyModel(field=\"null\")` would results in an empty dict `{}` which violates the promised schema. Given the model, I'd have expected the `field` to be required in validation mode, but optional in serialization mode.\n\nThis matters when using a code-first design for a JSON schema, e.g. when writing FastAPI applications. A potential workaround is to set a default value, but that's not quite equivalent.\n\nI used the below script to inspect schemas for for BaseModels and Dataclasses. In its output, I would expect the `'required': ['field']` item to be present in the input schemas but absent in the output schemas. It could perhaps make sense to include schema-level tests for the Exclude-If feature, in addition to the existing serialization tests ([models](https://github.com/pydantic/pydantic/blob//tests/test_main.py#L1389-L1404), [dataclasses](https://github.com/pydantic/pydantic/blob//tests/test_dataclasses.py#L3244-L3258)).\n\n(Anecdotally, I've also seen warnings like `Expected 3 fields but got 2` as triggered by [this Pydantic-Core code](https://github.com/pydantic/pydantic-core/blob//src/serializers/fields.rs#L238-L252), but I've been unable to reproduce via a minimal example.)\n\n### Example Code\n\n```Python\n# /// script\n# dependencies = [\n#     \"pydantic==2.12.3\",\n# ]\n# ///\n\nimport pprint\nfrom dataclasses import dataclass\nfrom typing import Annotated\n\nimport pydantic\n\n\ndef is_null_str(value: str) -> bool:\n    return value == \"null\"\n\n\nclass MyModel(pydantic.BaseModel):\n    field: Annotated[str, pydantic.Field(exclude_if=is_null_str)]\n\n\n@dataclass\nclass MyDataclass:\n    field: Annotated[str, pydantic.Field(exclude_if=is_null_str)]\n\nprint(\"=== BaseModel examples ===\")\nprint(\"Input schema:\")\npprint.pp(MyModel.model_json_schema(mode=\"validation\"))\nprint()\nprint(\"Output schema:\")\npprint.pp(MyModel.model_json_schema(mode=\"serialization\"))\nprint()\nprint(\"Pydantic Core schema:\")\npprint.pp(MyModel.__pydantic_core_schema__)\nprint()\nprint(\"Example output:\")\npprint.pp(MyModel(field=\"null\").model_dump(mode=\"json\"))\nprint()\n\nprint(\"=== Dataclass examples ===\")\nadapter = pydantic.TypeAdapter(MyDataclass)\nprint(\"Input schema:\")\npprint.pp(adapter.json_schema(mode=\"validation\"))\nprint()\nprint(\"Output schema:\")\npprint.pp(adapter.json_schema(mode=\"serialization\"))\nprint()\nprint(\"Pydantic Core schema:\")\npprint.pp(adapter.core_schema)\nprint()\nprint(\"Example output:\")\npprint.pp(adapter.dump_python(MyDataclass(field=\"null\"), mode=\"json\"))\nprint()\n```\n\n### Python, Pydantic & OS Version\n\n```Text\npydantic version: 2.12.3\n        pydantic-core version: 2.41.4\n          pydantic-core build: profile=release pgo=false\n               python version: 3.14.0 (main, Oct 10 2025, 12:47:49) [Clang 20.1.4 ]\n                     platform: Linux-6.8.0-85-generic-x86_64-with-glibc2.39\n             related packages: typing_extensions-4.15.0\n                       commit: unknown\n```"
}